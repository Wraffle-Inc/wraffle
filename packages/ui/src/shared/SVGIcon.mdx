import {
  Canvas,
  Meta,
  Primary,
  Controls,
  Description,
} from "@storybook/blocks";

import * as SVGIconStories from "./SVGIcon.stories";

<Meta of={SVGIconStories} />

# SVGIcon

SVGIcon은 SVG 이미지를 렌더링하는 컴포넌트입니다.

## Overview

<Primary />

## Props

<Controls />

## MenuIcon

<Canvas of={SVGIconStories.SVGIconMenu} meta={SVGIconStories} />
