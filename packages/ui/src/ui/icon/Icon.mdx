import {
	Canvas,
	Meta,
	Primary,
	Controls,
	Description
} from "@storybook/blocks";

import * as IconStories from "./Icon.stories";

<Meta of={IconStories} />

# Icon

Icon은 SVG 이미지를 렌더링하는 컴포넌트입니다.

## Overview

<Primary />

## Props

<Controls />

<br />

## Icon with Badge

`showBadge`이 true일 경우, 아이콘에 배지를 추가할 수 있습니다.

<Canvas of={IconStories.IconWithBadge} meta={IconStories} />

## Icon with BadgeCount

`showBadge`이 `true`이고 `badgeCount`의 값이 `0`보다 클 경우, 아이콘에 배지 카운트를 추가할 수 있습니다.

<Canvas of={IconStories.IconWithBadgeCount} meta={IconStories} />
